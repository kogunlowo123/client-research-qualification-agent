"""Criterion-by-criterion qualification: evidence registry, heuristic scorer and LLM agent.

Submodules are imported explicitly (``client_research_agent.qualification.agent`` etc.) so this
package stays free of import cycles with ``ranking`` and ``citations``.
"""
